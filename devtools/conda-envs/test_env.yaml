name: test
channels:

  - conda-forge
  - salilab
  - bioconda
  - defaults
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov
  
  - gromacs=2021.3
  - modeller

    # Pip-only installs
  #- pip:
  #  - codecov

